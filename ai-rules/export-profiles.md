# Export Profiles

This file stores saved export profiles for root rule 4.

Each profile is explicit shorthand for:

- one target agent
- one target environment
- zero or more toolset overlays

## Profile 1

- agent: `codex`
- environment: `windows`
- toolsets:
  - `typescript`
  - `sveltekit`
